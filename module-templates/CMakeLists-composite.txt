project([module-name] VERSION 0.1)

include_directories(.)

# add parts as required
#add_subdirectory(
# part 1
# part 2
#)

add_library([module-name]
    [module-name].h
    src/[module-name].cpp)

# add parts as required
#target_link_libraries([module-name]
# part 1
# part 2...
#)